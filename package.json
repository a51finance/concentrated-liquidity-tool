{
  "name": "@a51finance/concentrated-liquidity-tool",
  "description": "contracts for managing the liquidity on any concentrated AMM",
  "version": "1.0.0",
  "author": {
    "name": "a51finance",
    "url": "https://github.com/a51finance"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "solhint-community": "^3.6.0"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "forge",
    "foundry",
    "smart-contracts",
    "solidity",
    "template"
  ],
  "private": true,
  "scripts": {
    "clean": "rm -rf cache out",
    "lint": "pnpm lint:sol && pnpm prettier:check",
    "lint:sol": "forge fmt --check && pnpm solhint {script,src,test}/**/*.sol",
    "prettier:check": "prettier --check **/*.{json,md,yml} --ignore-path=.prettierignore",
    "prettier:write": "prettier --write **/*.{json,md,yml} --ignore-path=.prettierignore",
    "deploy-alp-goerli":"source .env && forge script script/deploy.s.sol:DeployALP --rpc-url $GOERLI_RPC_URL --broadcast --verify -vvvv"
  }
}
